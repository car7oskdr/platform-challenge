[project]
name = "platform-challenge"
version = "0.0.0" # la version real se toma de model/VERSION
description = "Mini-platform k3d: FastAPI + PostgreSQL con CI/CD y ArgoCD"
readme = "README.md"
authors = [
    { name = "car1100s", email = "carloscvl@hotmail.com" }
]
requires-python = ">=3.14,<3.15"
dependencies = [
    "asyncpg>=0.31.0",
    "fastapi>=0.141.1",
    "prometheus-client>=0.26.0",
    "uvicorn[standard]>=0.53.0",
]

[tool.uv]
package = false
